inherits = "catppuccin_mocha" # or your preferred theme

# 1. Force the main background to use the terminal's default (transparency)
"ui.background" = {}

# 2. Clear every background that would paint over it.
# Any background left set covers the image or blur underneath.
"ui.buffer" = {} 
"ui.statusline" = {}
"ui.statusline.inactive" = {}
"ui.help" = {}
"ui.popup" = {}
"ui.window" = {} 
"ui.menu" = {}
"ui.menu.selected" = {}
"ui.virtual.inlay-hint" = { fg = "gray" }
